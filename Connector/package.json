{
  "name": "mms-connector",
  "private": true,
  "version": "0.1.0",
  "description": "Scripted migration connector for Instatic CMS — emits a SiteBundle, imports it as draft, and releases it under an approval binding.",
  "type": "module",
  "engines": {
    "bun": ">=1.3.0 <1.4.0"
  },
  "scripts": {
    "connector": "bun run src/main.ts",
    "doctor": "bun run src/main.ts doctor",
    "test": "bun test"
  },
  "dependencies": {
    "turndown": "^7.2.0"
  }
}
