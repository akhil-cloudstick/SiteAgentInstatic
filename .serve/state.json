{"rev":60,"tasks":{"m1-docker":{"status":"done","notes":"Not needed - Instatic runs NATIVELY via Bun (no Docker / WSL2). This is the unlock."},"s9-instatic":{"status":"done","notes":"PROVEN LIVE - a native Instatic instance put all 33 tables into the tenant schema, 0 in public."},"s2-firstrun":{"status":"done","notes":"Resolved - tenant self-onboards via Instatic's native setup wizard (share link). No headless owner needed."},"s4-gateway":{"status":"in_progress","notes":"Gateway service + key-hiding design done; wiring each instance's AI base URL to it is the remaining step."},"s1-assets":{"status":"blocked","notes":"Blocked: Docker won't install on this machine (no admin / WSL2). Deferred to a Docker-capable machine."},"sA-anim":{"status":"blocked","notes":"Blocked: Docker won't install on this machine (no admin / WSL2). Deferred to a Docker-capable machine."},"s9-db":{"status":"done","notes":"Done - proven on real Postgres 18 (2026-06-30): isolation, doors locked, idempotent provision, clean teardown."},"s3-upload":{"status":"todo","notes":"Pending - testable WITHOUT Docker (wrangler + free Cloudflare account + a static test folder). The one remaining no-Docker spike."},"m1-postgres":{"status":"done","notes":"Done - siteagent_platform DB + siteagent_admin role created locally (2026-06-30)."},"m1-registry":{"status":"done","notes":"Built + migrated: siteagent_control (settings/tenants/deploys) + pg access + AES-256-GCM encrypted secrets."},"m1-compose":{"status":"done","notes":"Not needed - no Docker/compose; TenantRuntime spawns native Bun processes with per-tenant env."},"m2-schema-role":{"status":"done","notes":"Done - idempotent mint SQL proven (CREATE ROLE guard + CREATE SCHEMA IF NOT EXISTS + ALTER ROLE search_path)."},"m2-firstrun":{"status":"done","notes":"Instance boots + serves the setup wizard; tenant creates their own owner. Verified."},"m2-cf":{"status":"done","notes":"CF Pages project created lazily on first deploy (namespaced siteagent-slug)."},"m2-saga":{"status":"done","notes":"Provisioner saga + Registry state machine (db_ready, up, seeded, done) + cleanup on failure. Verified end-to-end via API."},"m2-deprovision":{"status":"done","notes":"Stops the process, removes the schema + role, clears the tenant folder. Built."},"m3-proxy":{"status":"done","notes":"AI Gateway built: forwards to OpenRouter, injects the global key, SSE streaming. Key never in a tenant instance."},"m3-token":{"status":"done","notes":"Signed per-tenant HMAC token, validated at the gateway (a leaked URL cannot be replayed)."},"m3-rate":{"status":"todo","notes":"Pending - per-tenant rate guard; gated by m3-proxy."},"m3-meter":{"status":"todo","notes":"Pending - usage metering to Registry; gated by m3-proxy + m1-registry."},"m4-watch":{"status":"in_progress","notes":"v1 uses an explicit Publish action in the console; auto file-watch is a follow-up."},"m4-deploy":{"status":"done","notes":"wrangler Direct Upload wired (project + deploy + capture pages.dev URL). Needs CF creds in Settings to run."},"m4-record":{"status":"done","notes":"Deploys saved in the registry with live URL + status."},"m4-domains":{"status":"todo","notes":"Pending - custom domains (CF API + DNS); gated by m4-deploy."},"m5-settings":{"status":"done","notes":"Astro SSR Settings page: OpenRouter key/model + Cloudflare creds (encrypted at rest, masked)."},"m5-tenants":{"status":"done","notes":"Astro SSR Tenants page: add / start / publish / remove; shows status, instance link, live URL. On :3000."},"m5-dash":{"status":"todo","notes":"Pending - usage/health dashboards; gated by m7-metrics."},"m5-alerts":{"status":"todo","notes":"Pending - failure alerts; gated by m1-registry."},"m6-roles":{"status":"todo","notes":"Pending - Tenant Admin + custom toggle roles; gated by m2-firstrun."},"m6-audit":{"status":"todo","notes":"Pending - per-user audit log; gated by m2-firstrun."},"m7-metrics":{"status":"todo","notes":"Pending - metrics collector + time series; gated by m1-registry."},"m8-upgrade":{"status":"todo","notes":"Pending - fleet upgrade (sequential rolling restart); gated by m2-saga."},"m8-backup":{"status":"todo","notes":"Pending - backup/restore (pg_dump -n + uploads); gated by m2-saga."},"m9-vps":{"status":"todo","notes":"Pending - production VPS (Linode) + Docker; gated by m5-tenants."},"m9-proxy":{"status":"todo","notes":"Pending - Caddy/Traefik editing-domain TLS; gated by m9-vps."},"m9-secrets":{"status":"todo","notes":"Pending - secrets manager (replace plaintext-on-host); gated by m5-settings."},"m9-pool":{"status":"todo","notes":"Pending - PgBouncer before ~5-8 tenants; gated by m1-postgres."}}}