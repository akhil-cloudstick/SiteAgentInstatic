{
  "title": "MMSBUILD delivery board",
  "source": "docs/prd/mmsbuild-prd.md",
  "note": "Modules are the PRD's phases, tasks are its requirements, subtasks are its acceptance criteria. Ids match the PRD exactly, so an acceptance run maps to this board line for line. `status` is ours — what is built. `verified` belongs to the acceptance run and is never set by us.",
  "legend": {
    "status": {
      "todo": "Not started",
      "doing": "In progress — work still to do here",
      "built": "Built, committed, and working",
      "awaiting-owner": "Finished here. Waiting on an action only the owner can take, named in the note.",
      "blocked": "Cannot proceed — something named in `blockedBy` has to be solved first"
    },
    "verified": {
      "pending": "Not yet run by the other party",
      "pass": "Acceptance run passed",
      "fail": "Acceptance run failed"
    }
  },
  "modules": [
    {
      "id": "P0",
      "title": "Phase 0 — Close the surface",
      "summary": "Every requirement after this one assumes an authenticated admin.",
      "tasks": [
        {
          "id": "R14",
          "title": "Admin authentication",
          "status": "built",
          "dependsOn": [],
          "note": "Built 2026-09-17: every admin route on the control plane and every console page requires a signed-in administrator — scrypt password, a signed 12-hour session scoped to the console, per-email lockout after repeated failures. One check sits ahead of every route, and a self-test fails if an admin route is ever left outside it. The connector keeps site creation only, on its own token. The board is no longer served on the public address. Live since 2026-09-17: on the public address every admin route answers 401 without a session, the console redirects to sign-in, and the public board path is gone.",
          "subtasks": [
            {
              "id": "AC-D14.1",
              "text": "Unauthenticated admin request refused; the console requires sign-in",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "NEW-1",
          "title": "The admin surface reveals no secret it does not have to",
          "status": "built",
          "dependsOn": [
            "R14"
          ],
          "note": "Built 2026-09-17: machine keys are stored as a hash only, the read-back route is gone, and stored copies are wiped at startup. The project listing carries no invite link, no AI gateway token and no secret-bearing field; a new invite link is shown once, when issued. Links already shared keep working until used or replaced. Live since 2026-09-17: a signed-in project listing carries no invite link, token or secret-bearing field, and a key read-back answers 404.",
          "subtasks": [
            {
              "id": "NEW-1a",
              "text": "A minted machine key cannot be read back in plaintext",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "NEW-1b",
              "text": "A project listing leaks no usable invite link or capacity token",
              "status": "built",
              "verified": "pending"
            }
          ]
        }
      ]
    },
    {
      "id": "P1",
      "title": "Phase 1 — The levels",
      "summary": "Operator and Business above Project, with roles that hold when the interface is bypassed.",
      "tasks": [
        {
          "id": "R1",
          "title": "Operator and Business levels above Project",
          "status": "built",
          "dependsOn": [
            "R14"
          ],
          "note": "Built 2026-09-17: Operators own Businesses, Businesses own projects, and a Business with no Operator sits directly under the platform. Every stored record carries its Operator and Business, stamped by the database, so moving a project or a Business re-addresses everything under it. Console accounts are scoped to the platform, one Operator or one Business; every read and change is limited to that scope, and a project outside it answers \"not found\". The chain is listed in the console's Organisation page and by the organisation API. The existing projects each became a Business of their own; the staging copy shares its site's. Project processes now hold only their own signing key and no control-plane credentials, so a project cannot reach another Business through its own server either. Builder smoke test 2026-09-17: an Operator and a Business under it were created in the console. The console was then redesigned to match the MMS-CMS — the shared MMS header, pages in setup order (Organisation → Projects → People → Access), dialogs for every create and change, confirmations for destructive actions, and themed dropdowns. Builder check 2026-09-18: the redesigned console was confirmed working. Two faults found in that check are fixed — a dropdown inside a dialog drew its options behind the dialog, so they were invisible, and the console was being served in development mode, which made every page fetch its source file by file. The console now runs from its build and each page asks the control plane for everything it needs at once: pages that took 1–2 seconds now answer in 15–30 ms.",
          "subtasks": [
            {
              "id": "AC-A1.1",
              "text": "The Operator → Business → Project chain is queryable; each level resolves its children",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-A1.2",
              "text": "Cross-Business read and write denied at the server; no foreign data in any listing",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "R3",
          "title": "Roles, and more than one person per project",
          "status": "built",
          "dependsOn": [
            "R1"
          ],
          "note": "Built 2026-09-17: a project has any number of people, each with one of the CMS's own four roles — Owner, Publisher (the CMS admin role), Author (the CMS client role: edits content, cannot publish) and Viewer. People are invited, re-roled and removed from the console's People page. The CMS enforces the role on every request, so a denial holds for a direct call as well as in the interface. A role change or removal reaches the CMS at once, including for a session that is already open. Live since the 2026-09-17 restart.",
          "subtasks": [
            {
              "id": "AC-A3.1",
              "text": "Publisher-role action allowed; the same action denied to an author role",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-A3.2",
              "text": "The denial holds when the interface is bypassed",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "NEW-2",
          "title": "Inviting a second person must not overwrite the first",
          "status": "built",
          "dependsOn": [
            "R1"
          ],
          "note": "Built 2026-09-17: an invite adds an account. Re-inviting someone who already has access is refused instead of locking them out; re-inviting someone still invited only replaces their link. When one email belongs to several projects, sign-in asks which project rather than picking one. Live since the 2026-09-17 restart.",
          "subtasks": [
            {
              "id": "NEW-2a",
              "text": "A second invite adds an account rather than replacing the first",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "NEW-3",
          "title": "A portal sign-in arrives with its own role, not as the owner",
          "status": "built",
          "dependsOn": [
            "R3"
          ],
          "note": "Built 2026-09-17: the hub's signed hand-off names the person and their role; the CMS signs them in as their own account with that role, and the design studio records who is working. Step-up is never pre-satisfied: it asks for the person's hub password, checked by the control plane, and an unreachable check fails. Sessions that the old hand-off opened with step-up already satisfied are ended at upgrade. The design studio's Share to CMS now brings the person into the CMS as themselves. Only the agent gateway and the studio's own server-side staging use a machine sign-in, which the hub never issues. Live since the 2026-09-17 daemon rebuild and restart.",
          "subtasks": [
            {
              "id": "NEW-3a",
              "text": "An author role is denied an owner-only action inside the CMS",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "NEW-3b",
              "text": "Step-up is demanded, not pre-satisfied",
              "status": "built",
              "verified": "pending"
            }
          ]
        }
      ]
    },
    {
      "id": "P2",
      "title": "Phase 2 — Scoped visibility and branding",
      "summary": "Counts without the work, and an Operator's own branding.",
      "tasks": [
        {
          "id": "R5",
          "title": "Platform Owner sees counts, not work; Operators are branded",
          "status": "built",
          "dependsOn": [
            "R1",
            "R3"
          ],
          "note": "Built 2026-09-18: the Platform Owner sees counts and status across the estate and opens no project by default. Four ways in were closed — minting an agent key, installing the bridge, inviting yourself to a project, and the unlinked route that put a project on a public address — and listing MCP keys no longer opens an owner session inside every project on each page load. Opening a business’s work is now \"Act as this business\": it asks why, records the answer before anything is handed over, puts platform staff on that project’s People list under their own name so the customer can see them, and ends after an hour or on exit — which removes the person, and so revokes the session in MMS-CMS and MMS-Design as well as the hub. Every administrator action is now recorded with the actor and, where it applies, who they were acting as; the trail is read on a new Activity page, scoped like everything else. Branding is per-Operator: logo, name and accent colour, set by upload or by pasting an address (fetched once and stored, so an agency’s customers never call a third-party server). It reaches the hub pages, both products’ headers, their browser tab titles and icons, and the console — with no per-Operator build, because the gateway answers the icon addresses the built pages already ask for and rewrites the title in the document it is already touching. A Business directly under the platform keeps MMSBUILD’s. The PRD’s AC-A5.2 referred to a list of \"branded surfaces\" that did not exist; it is authored in the PRD now (S1–S8).",
          "subtasks": [
            {
              "id": "AC-A5.1",
              "text": "Counts visible to the platform-owner role; opening the work denied",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-A5.2",
              "text": "Operator branding renders on the named surfaces",
              "status": "built",
              "verified": "pending"
            }
          ]
        }
      ]
    },
    {
      "id": "P3",
      "title": "Phase 3 — One project, one website",
      "summary": "A second design can never silently destroy the first.",
      "tasks": [
        {
          "id": "R2",
          "title": "A second website cannot silently destroy the first",
          "status": "built",
          "dependsOn": [
            "R1"
          ],
          "subtasks": [
            {
              "id": "AC-A2.1",
              "text": "A second design is refused or lands distinctly; site A still resolves with A's content",
              "status": "built",
              "verified": "pending"
            }
          ],
          "note": "Built 2026-09-18: a share now says which design it is carrying, and a website records the design it was built from — until now the handover was files and nothing else, so \"this design again\" and \"a different design\" arrived down one path looking identical. That is what destroyed the first website: the wizard empties the site before it plans the import, which is exactly why no conflict was ever detected and why the result read as a successful update. A different design into a project with a published website is now refused, before anything is staged, with a message naming how many pages are there and which design built them. A different design over unpublished work is not refused — iterating before launch is normal — but the wizard stops and names what would go, and nothing is touched until the person agrees. Re-sharing the SAME design still overwrites in place, unchanged: \"no duplicates on re-share\" depends on it, and it is the thing most easily broken by fixing the rest. The PRD referred to \"a different design\" and to \"already has a website\" without defining either; both are defined in the PRD now."
        },
        {
          "id": "R4",
          "title": "The CMS destination belongs to the project",
          "status": "built",
          "dependsOn": [
            "R1",
            "R2"
          ],
          "subtasks": [
            {
              "id": "AC-A2.2",
              "text": "A design's destination cannot be pointed at another project's CMS",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-A2.3",
              "text": "Publishing to A changes A's domain and not B's, verified by fetching both",
              "status": "built",
              "verified": "pending"
            }
          ],
          "note": "Built 2026-09-18: nothing ever chose the wrong CMS — the destination is one value per design-studio process, with no parameter, setting or screen — but nothing checked either: a mis-aimed push failed only because the receiving project’s key would reject the token, an accident rather than a rule. The CMS now refuses a hand-off that does not name it, and refuses one when it does not know which project it is, rather than accepting anything. The destination is also always stated rather than inherited, so a stray value in the control plane’s environment can no longer become a lite project’s CMS. On the domain side, the Cloudflare project and the custom domain were free text with no uniqueness rule, so two projects could name one destination — a typo, or a -staging clone keeping its source’s — and publishing either would have uploaded onto the other’s live domain; the database now refuses it and the console says which project already holds it. Separately, the design studio kept its Cloudflare credentials in a directory shared by every project on the host, letting any project’s studio attach any domain in that account; each project now keeps its own, and MMS-CMS stays the sole publisher (PRD §5.1)."
        }
      ]
    },
    {
      "id": "P4",
      "title": "Phase 4 — Governance, with proof",
      "summary": "NEW-4 first: nothing in the approver work functions until both ends agree.",
      "tasks": [
        {
          "id": "NEW-4",
          "title": "The issuing side resolves per-property approvers too",
          "status": "doing",
          "dependsOn": [
            "R1"
          ],
          "note": "Completed 2026-09-21: c865d10 gave both ends the same RULE over two separate configs — a file committed to git on one side, a Worker variable on the other, kept in step by a human comparing fingerprints. The PRD asks for something narrower: \"the registry is the only place this mapping lives, and both ends read the same registry\". That registry now exists, in the relay’s database: append-only, one live approver per property enforced by a partial unique index, with triggers refusing any edit or delete. The checking side reads it through a public endpoint — every field is a public key — and caches it, refreshed every few minutes and REFUSED once older than a GO can live, so a short relay outage changes nothing while a retired key can never outlive its own approvals. NOT LIVE YET. The relay on Cloudflare still runs a build from before the approver registry — asked for the registry it answers \"No such route\" — so this is written and committed but does nothing in production. Redeploying the relay is the owner's to run (it needs a Cloudflare login we do not have), and it also needs the D1 migration applied and a second Access Bypass policy for /api/approvers, without which that route returns a login page instead of the registry. Status stays blocked until the owner confirms the deploy and it has been checked. SECOND CORRECTION, 2026-09-21: this is not only waiting on the deploy — there is build work left here. The registration door is an owner-only HTTP route, and the owner on this relay is a BROWSER identity: the role is matched on the email in a Cloudflare Access assertion, and a service token can never hold it. So the one party permitted to register an approver has no practical way to call the route — they would have to extract an Access token from a browser session to use curl. The route was built without the surface its only permitted caller can reach it through. It belongs on the relay's own owner page, beside the button where they already record a GO, and NOT in the operator console: that console's administrator is a different identity from this relay's owner, so it could not act as them even if it offered to.",
          "subtasks": [
            {
              "id": "NEW-4a",
              "text": "A property-specific approval is issued and then accepted by the checking side",
              "status": "doing",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "R6",
          "title": "Per-property approver: registration, delegation, rotation",
          "status": "doing",
          "dependsOn": [
            "R1",
            "NEW-4"
          ],
          "note": "Built 2026-09-21: registration writes one row with an effective-from stamp, and the append-only table is itself the audit record. Rotation retires the old row and inserts the new one in a single batch, so a property is never left with two live identities or none by accident, and the retired identity stops being accepted immediately rather than at the next restart — with history kept so an old receipt can still be explained. Revocation without a replacement leaves the property refusing everything. Delegation is explicit: a business-level approver reaches only the properties its registration names, never what a hierarchy implies. And the fallback is gone: a property with no registered approver is refused rather than handed to the platform-wide key that, until this change, approved every site on the estate — \"a fallback that widens scope on error is a master key by another name\". CORRECTION, 2026-09-21: this note previously read as though registration were a first-class operation from the start. It was not. The store methods and the read endpoint shipped in Phase 4, but there was no write route, so registering an approver still meant a developer running wrangler d1 by hand against the production database — one of the thirteen developer actions R13 exists to remove, and one that blocked a new project at step zero, since a project with no approver refuses every gated action. The owner-only write door (POST /api/approvers, POST /api/approvers/<property>/retire) landed in Phase 5, with registration, rotation and revocation each covered by a test that goes through the route rather than calling the store directly — which is how it looked finished while it was not. NOT LIVE YET. The relay on Cloudflare still runs a build from before the approver registry — asked for the registry it answers \"No such route\" — so this is written and committed but does nothing in production. Redeploying the relay is the owner's to run (it needs a Cloudflare login we do not have), and it also needs the D1 migration applied and a second Access Bypass policy for /api/approvers, without which that route returns a login page instead of the registry. Status stays blocked until the owner confirms the deploy and it has been checked. SECOND CORRECTION, 2026-09-21: this is not only waiting on the deploy — there is build work left here. The registration door is an owner-only HTTP route, and the owner on this relay is a BROWSER identity: the role is matched on the email in a Cloudflare Access assertion, and a service token can never hold it. So the one party permitted to register an approver has no practical way to call the route — they would have to extract an Access token from a browser session to use curl. The route was built without the surface its only permitted caller can reach it through. It belongs on the relay's own owner page, beside the button where they already record a GO, and NOT in the operator console: that console's administrator is a different identity from this relay's owner, so it could not act as them even if it offered to.",
          "subtasks": [
            {
              "id": "AC-B6.1",
              "text": "P's approver accepted; another property's approver refused for the same action",
              "status": "doing",
              "verified": "pending"
            },
            {
              "id": "AC-B6.2",
              "text": "After rotation: old identity refused, new identity accepted",
              "status": "doing",
              "verified": "pending"
            },
            {
              "id": "AC-B6.3",
              "text": "No identity satisfies two properties — a key approving P1 fails on P2",
              "status": "doing",
              "verified": "pending"
            },
            {
              "id": "AC-B6.4",
              "text": "Business approver covers its own projects, never another Business's",
              "status": "doing",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "R7",
          "title": "Import and publish each gated, bound, and gated separately",
          "status": "built",
          "dependsOn": [
            "R6"
          ],
          "note": "Completed 2026-09-21: the exemption list is removed from the policy, the loader and the gate. A staging project is now gated exactly like a production one, because \"a staging project exempted from the approval gate proves nothing\" — the loop being rehearsed has to be the real one. The type that carried the gate result can no longer express \"passed without an approval\" at all, so the exemption cannot be reintroduced by configuration. NOTE 2026-09-21: the gate itself runs here and refuses correctly, but granting an approval needs the approver registry, which is not deployed (see R6). So the refusal half is provable today and the success half — AC-B7.5, import and publish under separate approvals — is not, until the relay is redeployed.",
          "subtasks": [
            {
              "id": "AC-B7.1",
              "text": "Import with no approval: refused, nothing imported",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B7.2",
              "text": "Approval bound to a different artefact: refused",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B7.3",
              "text": "Expired approval: refused",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B7.4",
              "text": "A spent approval presented again: refused",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B7.5",
              "text": "Import and publish need separate approvals; rows inspectable between them",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B7.6",
              "text": "B7.1–B7.4 still refused when the caller holds valid machine credentials",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "R8",
          "title": "Fail-closed everywhere",
          "status": "built",
          "dependsOn": [
            "R7"
          ],
          "note": "Completed 2026-09-21: the share’s compliance check no longer fails open. It used to pre-seed the outgoing files with the unchecked input and swallow any error into a warning, so a crash in the checker produced an HTTP 200 with a redirect — byte-identical to a clean pass — and staged the unchecked site. The verdict had no way to say \"could not check\": it now does, per page, so one unparseable page cannot destroy the verdict on the others. A crash and an unjudged page both block, under their own error code, distinguishable from a compliance failure and from success. Separately the model-capability check, which deliberately allowed a save through when the catalogue could not be read, now refuses — that was a check reporting a pass because it could not run, which principle P2 forbids in writing.",
          "subtasks": [
            {
              "id": "AC-B8.1",
              "text": "A crashed importability check blocks, distinguishably from a success",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B8.2",
              "text": "Missing or unreadable policy: every gated action refuses",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B8.3",
              "text": "A check that cannot run yields failure, never a zero-findings pass",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "R9",
          "title": "The proof chain: receipt, live verification, known-good, immutability",
          "status": "built",
          "dependsOn": [
            "R7"
          ],
          "note": "Built 2026-09-21: the publish now says what it published. The content hash was computed to check If-Match and then discarded, and the deploy webhook carried no body, so nothing about WHAT went live ever reached the side that recorded the deploy — which is why a receipt could name a time and a URL and nothing else. Receipts are a new append-only table whose triggers refuse any later edit or delete, carrying the content identity, the page count, the Cloudflare project and deployment id, the verification result, the previous receipt, the retained bundle and the build that did it. \"Live\" is no longer a regex over the deploy tool’s output: the site is fetched afterwards and each sampled route must serve ITS OWN page, so a host answering its fallback under a 200 is caught — an HTTP-200 check is explicitly not sufficient. A check that cannot run reports \"unverified\", never success. The bundle that was uploaded is copied out after the deploy writes its root files, because the CMS keeps two generations and destroys the older one as the first step of the next publish, so there was no spare generation to point at.",
          "subtasks": [
            {
              "id": "AC-B9.1",
              "text": "A receipt per publish with every named field present",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B9.2",
              "text": "Verification fetches the site; a missing route serving a fallback is caught as FAIL",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B9.3",
              "text": "A known-good bundle is retained and reconstructs the prior state",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B9.4",
              "text": "Editing or deleting a past receipt is denied",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "R10",
          "title": "The two ends do not trust each other",
          "status": "built",
          "dependsOn": [
            "R6"
          ],
          "note": "Held and reinforced 2026-09-21: the two implementations still share no code. The shared vectors covered only single-key signature checks, so per-property resolution was asserted twice in two suites that share nothing — the moment R6 landed, the two resolvers could have drifted with all nine signature cases still green. The vectors now carry nine resolution cases as well (own approver, unregistered property, empty registry, delegation covering and not covering, project winning over business, an unusable key, a retired approver), and both sides run them against their own resolver.",
          "subtasks": [
            {
              "id": "AC-B10.1",
              "text": "Verification succeeds with the issuing side offline, using unshared code",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-B10.2",
              "text": "A tampered approval fails on the acting side; both sides pass the shared suite",
              "status": "built",
              "verified": "pending"
            }
          ]
        }
      ]
    },
    {
      "id": "P5",
      "title": "Phase 5 — The repeatable push",
      "summary": "A new site pushed with zero developer round trips.",
      "tasks": [
        {
          "id": "R11",
          "title": "Pre-flight is the gate",
          "status": "built",
          "dependsOn": [
            "R8"
          ],
          "note": "Built 2026-09-21: the pre-flight is now a decision, not a report. It carries `ok` and coded findings alongside the sentences it always had, and a blocking finding refuses the import before anything reaches the CMS. Gating one call site was not enough — four paths could land a bundle, and two of them (archive and draft import) computed nothing at all; every path now projects, and the publish path gates too, against the draft as the CMS actually holds it, so a site that would bake broken is caught however its content arrived — bundle import, draft import, or a person editing in the admin. A check that could not run refuses rather than passing. There is no override. Two real bugs surfaced on the way: the projection compared rule ids against rule names, which made both \"kept for\" lists silently empty for every real bundle (the fixture set name === id, so the suite agreed with itself); and it predicted routes the bake provably never writes — template pages, unpublished rows, and rows of a table with no entry template. The threshold stays at a quarter, deliberately: PRD 5.5 warns against calibrating it against the wrong build, so v4 (8 of 617) and v7 (600 of 666) are both pinned as fixtures — the first must block, the second must pass.",
          "subtasks": [
            {
              "id": "AC-C11.1",
              "text": "A known-broken bundle is reported and blocked before import",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-C11.2",
              "text": "Surviving-rule count, dropped rules, routes and what-is-missing, all parseable",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-C11.3",
              "text": "Predicted routes equal published routes",
              "status": "built",
              "verified": "pending",
              "note": "Prediction and bake now share one implementation of a site's URL shape rather than two that were free to drift, and the publish reports the routes it actually baked, so the prediction is compared against reality instead of against itself."
            }
          ]
        },
        {
          "id": "R12",
          "title": "Fonts: idempotent install, and survival across a replace",
          "status": "built",
          "dependsOn": [],
          "note": "Built 2026-09-21: a replace no longer loses the fonts the new design still uses. The design blank stays — it is what stops a re-share accumulating stale fonts from previous shares — but the registry is now reconciled across it rather than simply dropped: a family the incoming design actually references comes back, and one it no longer uses does not. References are read from style rules, from breakpoint variants, and from raw stylesheets, which is where a hand-written design keeps its typography. Tokens return only when their family does, since a token pointing at a missing family resolves to a bare fallback stack — the unstyled result the whole requirement is about. The font files were never deleted by any of this, so what was lost was metadata and what is restored points at bytes that are still there.",
          "subtasks": [
            {
              "id": "AC-C12.1",
              "text": "Font install returns ids; re-running changes nothing",
              "status": "built",
              "verified": "pending"
            },
            {
              "id": "AC-C12.2",
              "text": "Intended fonts render on the live page after a replace-style import",
              "status": "built",
              "verified": "pending",
              "note": "connector_list_fonts reports installed families and what each token resolves to, so \"the font survived the replace\" is something a machine can assert instead of something somebody looks at."
            }
          ]
        },
        {
          "id": "R-SEO",
          "title": "Per-page SEO renders unconditionally",
          "status": "built",
          "dependsOn": [],
          "note": "Built 2026-09-21: better than this board said, and now complete. Title, description, canonical, Open Graph and JSON-LD were already emitted by the core publisher on the only public render path, with no capability check and no plugin — there is no SEO plugin in the repo at all. What was missing was the schema: five of the seven fields had to be added per project by a connector call, so a site that skipped the step emitted no canonical tag. They are part of the stock pages and posts schema now, for new projects and existing ones alike, and the field names match what the renderer already reads.",
          "subtasks": [
            {
              "id": "AC-C-SEO",
              "text": "SEO fields render in the served page from the core renderer, no add-on",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "R13",
          "title": "A staging project the studio drives itself",
          "status": "awaiting-owner",
          "dependsOn": [
            "R11",
            "R12"
          ],
          "subtasks": [
            {
              "id": "AC-C13.1",
              "text": "Studio completes preview → import → publish with zero developer actions",
              "status": "awaiting-owner",
              "verified": "pending"
            },
            {
              "id": "AC-C13.2",
              "text": "Milestone — a new site pushed with zero developer round trips",
              "status": "awaiting-owner",
              "verified": "pending",
              "note": "Asserted as arithmetic rather than description: the test records who acted, and the milestone is the assertion that the developer list is empty while the owner signed exactly twice."
            }
          ],
          "note": "Built 2026-09-21. One operation runs the whole sequence PRD 5.4 names — pre-flight, import under the owner-signed GO, inspect, publish under a second GO — and it drives the ordinary import and publish tools rather than reimplementing them, so it passes exactly the gates a person would. An orchestrator that called the CMS directly would be a way around those gates, which is the same as not having them. It never blocks on a person: each call advances as far as it can and reports whose move it is, state lives on disk so the wait outlives the process, and the relay's own replay means a resumed step reuses the ticket it already opened instead of putting a second deploy-request in front of the owner. A parked run resumes by itself once the signature exists — by polling, because the relay is a Worker and the Connector listens on loopback behind the gateway, so the webhook that looks more elegant is the one that cannot be delivered. A bundle the pre-flight refuses stops the run BEFORE anyone is asked to approve it. Two things outside the loop were what actually kept the count above zero, and both are fixed: the stack's own launcher read its credentials from whatever shell happened to start it rather than from platform configuration — the literal reading of \"without a developer holding the credentials\" — and a service that exited stayed dead until somebody noticed, which is a developer round trip by any measure. Services now restart with backoff, and configuration comes from the platform's own file. Project creation also says plainly that a new project has no approver and how the owner registers one, instead of letting a first push fail at its first approval with a message about a registry nobody mentioned. ONE THING DELIBERATELY NOT DONE: the studio's Share-to-CMS path still ends at a browser wizard. That is a decision on record, not an omission — the browser runs the same wizard a manual drag-drop uses, so Share to CMS cannot drift from manual import, and a server-side adapter would be a second implementation of the importer. It costs no DEVELOPER action, which is what this requirement counts; the studio operating its own tool is the studio doing its job. PENDING, and this status was wrong before: it was set to built while the live relay still answered \"No such route\" for the registry the loop depends on. The loop opens deploy-requests and reads GOs on that relay, so until it is redeployed the push cannot run against production at all — everything asserted above is asserted against tests and a local stack. It moves to built when the owner has deployed and a real push has been run end to end. STATUS, corrected again: \"blocked\" overstated it — nothing here is stuck. The loop is written, committed and passing; what it waits on is the relay redeploy, which needs a Cloudflare login the owner holds and we do not, and an approver registered for whatever project it pushes to (see R6). It moves to built when both are true and a real push has run end to end.",
          "blockedBy": [
            "relay redeploy on Cloudflare (owner)",
            "an approver registered for the target"
          ]
        }
      ]
    },
    {
      "id": "P6",
      "title": "Phase 6 — Productionisation",
      "summary": "Safe replace, complete clear-all, and an honest cold start.",
      "tasks": [
        {
          "id": "R15",
          "title": "Safe replace, and a complete clear-all",
          "status": "doing",
          "dependsOn": [
            "R9"
          ],
          "note": "The clear-all covers all four stores and snapshots first, but nothing reads that snapshot back and the replace path takes no snapshot at all.",
          "subtasks": [
            {
              "id": "AC-D15.1",
              "text": "Prior published state recoverable after a replace",
              "status": "todo",
              "verified": "pending"
            },
            {
              "id": "AC-D15.2",
              "text": "A deleted page stays gone from every store after clear-all",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "R16",
          "title": "Cold-start clarity",
          "status": "doing",
          "dependsOn": [],
          "note": "The waiting page is correct; the administrative view collapses \"not up yet\" into \"not installed\".",
          "subtasks": [
            {
              "id": "AC-D16.1",
              "text": "A cold studio returns \"starting\", not something that reads as \"not installed\"",
              "status": "todo",
              "verified": "pending"
            }
          ]
        }
      ]
    },
    {
      "id": "E",
      "title": "Security acceptance — the locked ten",
      "summary": "Run by someone other than the builder, on a throwaway or staging project, never on a live site. Five have no control at all today.",
      "tasks": [
        {
          "id": "E1",
          "title": "Secret access blocked",
          "status": "built",
          "dependsOn": [
            "NEW-1"
          ],
          "note": "Covered by NEW-1, built 2026-09-17.",
          "subtasks": [
            {
              "id": "E1",
              "text": "Covered by NEW-1a / NEW-1b",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "E2",
          "title": "Network and tool allowlists enforced",
          "status": "todo",
          "dependsOn": [],
          "note": "No test and no control today — net-new.",
          "subtasks": [
            {
              "id": "E2",
              "text": "New test needed",
              "status": "todo",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "E3",
          "title": "Prompt-injection attempts rejected",
          "status": "todo",
          "dependsOn": [],
          "note": "No test and no control today — net-new.",
          "subtasks": [
            {
              "id": "E3",
              "text": "New test needed",
              "status": "todo",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "E4",
          "title": "Spending limits enforced",
          "status": "todo",
          "dependsOn": [],
          "note": "No spend cap exists. An accepted omission until now; to become a real control.",
          "subtasks": [
            {
              "id": "E4",
              "text": "New test and a real spend cap needed",
              "status": "todo",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "E5",
          "title": "Cross-account access blocked",
          "status": "built",
          "dependsOn": [
            "R1"
          ],
          "note": "Covered by AC-A1.2, built 2026-09-17.",
          "subtasks": [
            {
              "id": "E5",
              "text": "Covered by AC-A1.2",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "E6",
          "title": "Expired and replayed permits rejected",
          "status": "built",
          "dependsOn": [
            "R7"
          ],
          "note": "Covered by AC-B7.3 and AC-B7.4.",
          "subtasks": [
            {
              "id": "E6",
              "text": "Covered by AC-B7.3, AC-B7.4",
              "status": "built",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "E7",
          "title": "CSRF and secure-cookie behaviour",
          "status": "todo",
          "dependsOn": [],
          "note": "No test and no control today — net-new.",
          "subtasks": [
            {
              "id": "E7",
              "text": "New test needed",
              "status": "todo",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "E8",
          "title": "Unsafe HTML and script input neutralised",
          "status": "todo",
          "dependsOn": [],
          "note": "No test and no control today — net-new.",
          "subtasks": [
            {
              "id": "E8",
              "text": "New test needed",
              "status": "todo",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "E9",
          "title": "Audit-record tampering prevented",
          "status": "doing",
          "dependsOn": [
            "R9"
          ],
          "note": "Covered by AC-B9.4; the approval store is already append-only.",
          "subtasks": [
            {
              "id": "E9",
              "text": "Covered by AC-B9.4",
              "status": "doing",
              "verified": "pending"
            }
          ]
        },
        {
          "id": "E10",
          "title": "Failed deployment and rollback exercised",
          "status": "todo",
          "dependsOn": [
            "R9"
          ],
          "note": "Covered by AC-B9.3 plus a rollback receipt; no rollback exists today.",
          "subtasks": [
            {
              "id": "E10",
              "text": "Covered by AC-B9.3, plus a rollback receipt",
              "status": "todo",
              "verified": "pending"
            }
          ]
        }
      ]
    }
  ]
}
