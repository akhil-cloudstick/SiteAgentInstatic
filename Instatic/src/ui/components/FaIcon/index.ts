/**
 * FaIcon — now owned by `@mms/shell`, which also vendors the Font Awesome
 * Solid webfont it renders from. Re-exported here so no call site changed.
 *
 * Everywhere outside the MMSBUILD-approved screens, keep using `RemixIcon` /
 * the `pixel-art-icons` components — that rule is unchanged by the move.
 */
export { FaIcon } from '@mms/shell'
