{
  "name": "pixel-art-icons",
  "version": "0.0.0-vendored",
  "description": "Vendored icon set for MMS-CMS. Components render the Remix Icon line-icon set (MMS Design System) via the remixicon webfont; the pixel-art-icons name/specifier + IconComponent contract are retained so existing imports resolve unchanged. Rename to an honest specifier is a follow-up.",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    "./types": {
      "types": "./dist/types.d.ts",
      "default": "./dist/types.js"
    },
    "./icons/*": {
      "types": "./dist/icons/*.d.ts",
      "default": "./dist/icons/*.js"
    }
  },
  "peerDependencies": {
    "react": ">=18"
  }
}
